const API = {
	LOGIN: "/auth/login",
	PROFILE: "/user/profile",
	EMPLOYESS: "/employees",
	ATTENDANCE: "/attendance",
	PROJECTS: "/projects",
	PROJECT_WORK_LOGS: "/project-work-logs",
} as const;

export default API;
